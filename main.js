let angle = 0;
let lineLength = 100;
let timer = 0;
let phase = 1;
let circleX = 200;
let circleSize = lineLength * 2;
let angleSecondary = 0;
let circleXSecondary = 200;
let circleSizeSecondary = lineLength * 2;
let innerCircleSize = 20;
let growing = true;
let canvas;

function setup() {
  canvas = 
  canvas = create

  canvas

 
createCanvas(400, 400);
  canvas.
  canvas

 
mouseOver(overCanvas);
  canvas.
 
mouseOut(outCanvas);
  
  frameRate
frameRate(60);
}

function draw() {
  background(220);

  

 


// ...

  

 


if (phase === 1) {
    // ...

  } else if (phase === 2) {
    
   
// ...

  } else if (phase === 3) {
    // ...

  }
}

function overCanvas() {
  canvas.style('display', 'none');
}

function outCanvas() {
  canvas.style('display', 'block');
}